---
pagination:
  data: remoteNews
  size: 1
  alias: article
permalink: "{{ article.url }}"
layout: article.njk
eleventyComputed:
  date: "{{ article.date }}"
  title: "{{ article.title }}"
---

{{ article.content | safe }}
